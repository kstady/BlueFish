services: 
  app:
    image: ghcr.io/ximeraproject/ximeralatex:v2.7.9-full
    volumes:
      - ..:/code:cached
    command: /bin/sh -c "while sleep 1000; do :; done"
    network_mode: "service:ximeraserver"
  ximeraserver:
    image: ghcr.io/ximeraproject/ximera-docker:latest
    ports:
      - "2080:2000"